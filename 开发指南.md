# 1.后端
  1. StateBackend -> 内存中存储
  2. StoreBackend -> 配置数据库进行存储
  3. FileSystemBackend -> 配置文件系统进行存储
  4. CompositeBackend -> 路由器，搭配其余后端进行存储
```
from deepagents import create_deep_agent
from deepagents.backends import CompositeBackend, StateBackend, StoreBackend
from langgraph.store.memory import InMemoryStore

agent = create_deep_agent(
    model="google_genai:gemini-3.5-flash",
    backend=CompositeBackend(
        default=StateBackend(),
        routes={
            "/memories/": StoreBackend(namespace=lambda _rt: ("memories",)),
            "/policies/": StoreBackend(namespace=lambda rt: (rt.context.org_id,),),
        },
    ),
    store=InMemoryStore(),  # store 传给 create_deep_agent，不是传给 backend
    # 设置权限
    permissions=[
        FilesystemPermission(
            operations=["write"],
            paths=["/policies/**"],     #  设置路径超出管理的文件的拒绝策略
            mode="deny",
        ),
    ],
)

```
# 2.权限
  1. 权限配置可支持的操作   ls, read_file, write_file, glob, grep, edit_file, delete；其中read等价于（read_file, ls）, write包括（write_file, edit_file）
  
  2. 权限模式：allow, deny, interrpute
```
# 首个匹配胜出，规则顺序很重要。将更具体的规则放在更宽泛的规则之前
permissions=[
    FilesystemPermission(
        operations=["read", "write"],
        paths=["/workspace/.env", "/workspace/examples/**"],
        mode="deny",
    ),
    FilesystemPermission(
        operations=["read", "write"],
        paths=["/workspace/**"],
        mode="allow",
    ),
    FilesystemPermission(
        operations=["read", "write"],
        paths=["/**"],
        mode="deny",
    ),
],
```

# 实现问题
  1. 做成 Codex / Claude Code 同类产品：同一套 agent harness，CLI + Desktop 双端
  2. 前端要有完整界面（会话、流式对话、工具卡片、diff、审批、Skills、设置），不是只暴露 Python API
  3. 沙箱 docker
  4. Skills 管理：公共只读、个人可编辑，两端都能看/管
  5. Human-in-the-loop：两端同一套审批协议

# 目标

基于 deepagents 做本地 coding agent 产品（对标 Codex / Claude Code）：

- **Agent Core**：deepagents 循环（文件、shell、skills、权限、HITL）
- **App Server**：长驻进程，会话/轮次的唯一真相；CLI 和 Desktop 都只跟它说话
- **CLI**：终端 TUI + 一次性命令
- **Desktop**：原生壳 + 共享 Web 前端

UI 和 TUI **禁止**直接 `create_deep_agent` / `invoke`。只走 App Server 协议。

仓库只分两个工程：`backend/`（conda / Python）、`frontend/`（nvm / Node）。不要 `src/`、`apps/`。根目录只留文档和忽略规则。

```
deepAgents/
├── .gitignore
├── .nvmrc
├── README.md
├── AGENTS.md
├── 开发指南.md
├── docs/
│   ├── architecture.md
│   └── api.md
│
├── backend/                        # Python（conda activate deepagents）
│   ├── pyproject.toml
│   ├── uv.lock
│   ├── .env.example
│   ├── README.md
│   ├── protocol/                   # JSON-RPC
│   │   ├── methods.py
│   │   └── events.py
│   ├── agent/                      # deepagents 核心，仅 server 调用
│   │   ├── factory.py
│   │   ├── backends.py
│   │   ├── permissions.py
│   │   ├── sandbox.py
│   │   ├── prompts.py
│   │   └── tools/web.py
│   ├── server/                     # App Server
│   │   ├── app.py
│   │   ├── rpc.py
│   │   ├── session.py
│   │   └── stream.py
│   ├── cli/harness_cli/            # CLI / TUI
│   │   ├── main.py
│   │   ├── client.py
│   │   └── tui.py
│   ├── config/
│   │   ├── default.yaml
│   │   ├── production.yaml
│   │   └── schema.py
│   ├── skills/example-skill/SKILL.md
│   ├── subagents/researcher/AGENTS.md
│   ├── tests/
│   │   ├── conftest.py
│   │   ├── unit/test_protocol.py
│   │   ├── unit/test_agent.py
│   │   ├── unit/test_rpc.py
│   │   └── integration/test_turn.py
│   └── scripts/
│       ├── gen_protocol.py
│       └── eval.py
│
└── frontend/                       # Node（nvm use 22）
    ├── package.json                # workspaces: web + desktop
    ├── package-lock.json
    ├── .nvmrc
    ├── web/                        # @harness/web，共享 React
    │   ├── package.json
    │   ├── vite.config.ts
    │   ├── index.html
    │   └── src/
    │       ├── main.tsx
    │       ├── App.tsx
    │       ├── protocol/types.ts
    │       ├── lib/rpc.ts
    │       ├── pages/              # Chat / Sessions / Skills / Settings
    │       └── components/         # MessageList / Composer / ToolCard / Diff / Approval
    └── desktop/                    # @harness/desktop，Electron 壳
        ├── package.json
        ├── tsconfig.json
        └── electron/
            ├── main.ts
            └── preload.ts
```

日常命令：

```powershell
# 后端
conda activate deepagents
cd backend
pip install -e ".[dev]"

# 前端
cd frontend
nvm use 22
npm install
npm run dev:web
```

# 3. 实现逻辑（产品形态）

对标 Codex App Server：把 harness 和界面拆开。

```
CLI TUI  ──┐                      ┌── Agent Core (deepagents)
           ├── App Server ────────┤     CompositeBackend
Desktop ───┘   thread / turn      │     permissions / HITL
  └─ frontend React               └── Docker sandbox / 本地盘
```

- App Server：长驻。管 thread、把 stream 打成协议事件、把审批打回 `Command(resume=...)`。
- CLI：子进程拉起 server（stdio JSON-RPC），或连已有 daemon。
- Desktop：壳里 spawn 同一份 server，前端走 preload / 本地 WebSocket。
- Agent Core：无 UI 知识。

传输（先做前两个）：

| 传输 | 谁用 | 格式 |
| --- | --- | --- |
| stdio JSONL | CLI、Desktop 子进程 | 一行一条 JSON-RPC |
| `ws://127.0.0.1` | 前端热更新、远程 TUI | 一帧一条 |
| unix/named pipe | 本机 daemon 复用 | 可选，后做 |

## 3.1 协议（两端唯一接口）

JSON-RPC 2.0，双向。客户端调方法；服务端推 notification；**审批是服务端发起的 request**，客户端必须回。

握手：每条连接先 `initialize`，再发 `initialized`。未握手禁止其它方法。

**Thread**（一场会话） / **Turn**（用户一句到 agent 停） / **Item**（回合里一条可渲染块）。

客户端 -> 服务端：

| 方法 | 作用 |
| --- | --- |
| `initialize` | 版本、cwd、client 类型（`cli` / `desktop`） |
| `thread/start` | 新会话。参数：`workspace`、`sandbox` |
| `thread/resume` | 按 `thread_id` 续聊 |
| `thread/list` | 会话列表（Sessions 页） |
| `thread/archive` | 归档 |
| `turn/start` | 用户输入，开始一回合 |
| `turn/interrupt` | 取消当前回合 |
| `approval/resolve` | 人审结果：approve / edit / reject / respond |
| `skills/list` | 公共 + 个人 |
| `skills/read` | 读某个 `SKILL.md` |
| `skills/write` | 只允许个人库 |
| `config/get` `config/set` | 模型、权限、sandbox |

服务端 -> 客户端（notification）：

| 事件 | 前端/TUI 怎么画 |
| --- | --- |
| `thread/started` `thread/updated` | 会话列表 |
| `item/started` `item/delta` `item/completed` | 对话流 |
| `turn/completed` `turn/failed` | 结束态、错误条 |
| `error` | toast / stderr |

Item 类型（和 Codex 同类，方便双端同一套 renderer）：

| `type` | 内容 |
| --- | --- |
| `user_message` | 用户文本 |
| `agent_message` | 模型正文，`delta` 流式 |
| `reasoning` | 可选思考 |
| `command_execution` | shell：命令、stdout、exit |
| `file_change` | 写/改文件 + diff |
| `tool_call` | 其它工具（web、task） |
| `skill_use` | 激活了哪个 skill |

审批（服务端 request，不是 notification）：

```
server -> client: approval/request
  { request_id, tool, args, allowed_decisions }
client -> server: approval/resolve
  { request_id, decision, edited_args?, message? }
```

CLI：阻塞提示（y/n/edit）。Desktop：`ApprovalDialog` 模态。同一 JSON，禁止各写一套审批语义。

`backend/scripts/gen_protocol.py`：Pydantic -> `frontend/web/src/protocol/types.ts`。类型只维护 Python 一侧。

## 3.2 App Server

`backend/server/` 是进程入口，不是 Web 业务后台。包名是 `server`（`python -m server`）。

```
app.py
  读传输（stdio 或 ws）
  -> rpc.py 分发
       initialize / thread / turn / skills / config
  -> session.py
       thread_id -> checkpointer + agent 实例
  -> stream.py
       agent.stream(...) -> item/delta
       interrupt -> 发出 approval/request，挂起等 resolve
```

`session.py`：

- `thread_id` = LangGraph `configurable.thread_id`
- 每 thread 一个 agent（或按 workspace 复用 factory，state 靠 checkpointer）
- `thread/list` 读持久化元数据（标题、cwd、更新时间），不要扫全部 checkpoint blob

`stream.py`：

1. `turn/start` -> `agent.astream(..., stream_mode=["messages", "updates"])`
2. token -> `item/delta`（`agent_message`）
3. 工具开始/结束 -> `command_execution` / `file_change` / `tool_call`
4. 图 interrupt -> 停 stream，发 `approval/request`，等 `approval/resolve`，再 `Command(resume=...)`
5. `turn/interrupt` -> cancel 当前 task，补齐未完成 item

约束：

- 只绑本机回环；远程必须鉴权后再开
- 有界队列，满了回 `-32001`（overload）
- Desktop / CLI 可同时连同一 daemon，按 `thread_id` 隔离；同一 thread 同时只跑一个 turn

## 3.3 Agent Core（仅 server 调用）

`backend/agent/factory.py` 组 `create_deep_agent`。逻辑仍用第 1、2 节的 backend / 权限。

虚拟路径：

| 前缀 | Backend | 写 |
| --- | --- | --- |
| `/workspace/` | Docker sandbox 或本地 `FilesystemBackend` | 允许，高危 HITL |
| `/skills/shared/` | 只读盘或 org `StoreBackend` | deny |
| `/skills/personal/` | user `StoreBackend` 或可写盘 | 允许，可 interrupt |
| `/memories/` | `StoreBackend` | 允许 |
| 其它 | `StateBackend` default | 线程临时文件 |

```python
def create_harness_agent(cfg, *, store, checkpointer, context):
    return create_deep_agent(
        model=cfg.model,
        backend=build_backend(cfg, context=context),
        store=store,
        skills=["/skills/shared/", "/skills/personal/"],
        permissions=build_permissions(cfg),
        interrupt_on=build_interrupt_on(cfg),
        checkpointer=checkpointer,
        system_prompt=SYSTEM_PROMPT,
        name="harness",
    )
```

Docker：`backend/agent/sandbox.py` 实现 `BaseSandbox`（`id` / `execute` / `upload_files` / `download_files`）。sandbox 只挂 `/workspace/`，不当 Composite default。`execute` 必须走审批。

Skills：公共只读、个人可写、`skills=` 个人在后（同名覆盖）。个人写入用 `mode="interrupt"`，两端弹同一审批。

权限首个匹配；`delete` 不含在 `write` 里。`execute` 路径规则管不住，靠容器隔离 + 审批。

## 3.4 CLI

入口：`harness`（`backend/cli/harness_cli/main.py`）。

```
harness                 # 默认 TUI，cwd = workspace
harness --workspace DIR
harness ask "..."       # 非交互一轮，审批走 stdin 或 --auto-approve（仅 dev）
harness serve           # 只起 App Server（Desktop / 远程 TUI 来连）
harness resume ID
```

启动：

1. 发现本机 daemon；没有就 spawn `python -m server`，stdio 互连
2. `initialize`（`client=cli`）
3. `thread/start` 或 `thread/resume`
4. TUI 画 item 流；`approval/request` 时全屏确认

TUI 最低块：消息流、当前工具/命令、输入框、审批面板。不在 CLI 里读盘执行命令。

## 3.5 前端 + Desktop

`frontend/web/` 一份 React。Desktop 加载它；开发可用浏览器连 `ws://127.0.0.1`。

页面：

| 页 | 协议 | 要显示 |
| --- | --- | --- |
| `SessionsPage` | `thread/list` `thread/start` `resume` | 历史会话、workspace、时间 |
| `ChatPage` | `turn/start` + item 事件 | 流式正文、工具卡、命令输出、文件 diff、中断按钮 |
| `SkillsPage` | `skills/*` | 公共只读列表；个人可编辑 `SKILL.md` |
| `SettingsPage` | `config/*` | 模型、sandbox 开关、权限摘要、工作区路径 |

组件：

- `MessageList`：按 item 渲染，不自己猜 tool 文本
- `ToolCard`：`command_execution` / `tool_call` 折叠
- `DiffView`：`file_change`
- `ApprovalDialog`：决策按钮跟 `allowed_decisions` 走
- `Composer`：回车 `turn/start`；进行中改成 interrupt

`lib/rpc.ts`：只发协议，不碰 agent。

Desktop 壳（`frontend/desktop`）：

1. 启动时 spawn App Server（打包进安装包的 Python/二进制，版本钉死）
2. stdio 或本机 ws 接到 preload
3. 窗口加载 frontend
4. 退出：关掉自己拉起的子进程；已有 daemon 则不断

窗口：主聊天 + 可选侧栏（Sessions / Skills / 文件树只读）。文件树经 server 列 `/workspace/`，不要让渲染进程直接 `fs`。

## 3.6 配置

`backend/config/schema.py`：

```python
class ClientConfig: ...
class SandboxConfig:
    enabled: bool = False
    image: str = "python:3.12-slim"
    network: str = "none"
    timeout_s: int = 60

class HarnessConfig:
    model: str
    workspace_root: str          # 绝对路径
    sandbox: SandboxConfig
    server_transport: str        # stdio | ws
    ws_bind: str = "127.0.0.1"
```

`default.yaml`：sandbox 关、HITL 开、本地盘。  
`production.yaml`：sandbox 开、checkpointer/store 持久化。  
密钥只进 `.env`。

## 3.7 实现顺序

1. `backend/protocol` + 单测编解码。`backend/scripts/gen_protocol.py` 出 TS 类型到 `frontend/web`。
2. App Server 空转：`initialize` / `thread/start` / 假 `turn/start` 回一条 `agent_message`。
3. CLI `harness ask` 走真协议（先假回复）。
4. `backend/agent/factory.py` 接上 deepagents；`server/stream.py` 把真 stream 映射成 item。
5. 审批走通：server request <-> CLI 提示 <-> `Command(resume=...)`。
6. `frontend/web` Chat + Approval；浏览器连 ws。
7. `frontend/desktop` spawn server + 加载 web。
8. Sessions / Skills / Settings 页。
9. Docker sandbox；`execute` 强制审批。
10. TUI 对齐 Desktop 的 item 渲染。

未做 sandbox 前不要在 CLI/Desktop 里直接起宿主机 shell。

# 附录 A. Agent Core 细节（路径对齐 `backend/`）

## A.1 虚拟路径约定

| 前缀 | Backend | 作用 | Agent 写权限 |
| --- | --- | --- | --- |
| `/workspace/` | Docker sandbox 或 `FilesystemBackend` | 任务工作区、脚本执行 | 允许，高危操作 HITL |
| `/skills/shared/` | `StoreBackend`（org 命名空间）或只读盘 | 公共 skill | deny write |
| `/skills/personal/` | `StoreBackend`（user 命名空间）或可写盘 | 个人 skill，可编辑 | 允许 write；可选 interrupt |
| `/memories/` | `StoreBackend` | 跨 thread 记忆 | 允许 write |
| `/policies/` | `StoreBackend` | 组织策略 | deny write |
| 其余（含 `/large_tool_results/`、`/conversation_history/`） | `StateBackend`（default） | 线程内临时文件 | 默认允许 |

`skills=` 只传目录容器路径，不传某个 `SKILL.md` 本身：

```
skills=["/skills/shared/", "/skills/personal/"]
```

同名 skill：后源覆盖前源。个人放后面，覆盖公共。

## A.2 模块对照

| 文件 | 写什么 |
| --- | --- |
| `backend/config/schema.py` | Pydantic：model、路径、权限、HITL、sandbox 开关 |
| `backend/config/default.yaml` / `production.yaml` | 默认值 / 生产值 |
| `backend/agent/factory.py` | `create_harness_agent`：唯一组装入口 |
| `backend/agent/backends.py` | 组 `CompositeBackend`；Docker sandbox 实现 |
| `backend/agent/tools/*.py` | 额外工具（web）。FS 内置工具不重复造 |
| `backend/agent/prompts.py` | 系统提示：路径约定、skill 用法、何时委派 |
| `backend/skills/` / `backend/subagents/` | 仓库种子；运行时以虚拟路径为准 |
| `backend/tests/unit/test_agent.py` | 工厂参数、路由、权限顺序 |
| `backend/tests/integration/test_turn.py` | skill 加载、HITL resume、sandbox execute |

## A.3 `create_harness_agent`

`backend/agent/factory.py` 伪逻辑：

```python
from deepagents import create_deep_agent
from langgraph.checkpoint.memory import MemorySaver  # 生产换 Postgres/Sqlite

from agent.backends import build_backend
from agent.prompts import SYSTEM_PROMPT
from agent.tools import build_tools


def create_harness_agent(cfg, *, store, checkpointer=None, context=None):
    backend = build_backend(cfg, store=store, context=context)
    permissions = build_permissions(cfg)
    tools = build_tools(cfg)
    subagents = build_subagents(cfg)

    return create_deep_agent(
        model=cfg.model,
        system_prompt=SYSTEM_PROMPT,
        backend=backend,
        store=store,                    # 给 StoreBackend，不传给 backend 构造器
        skills=["/skills/shared/", "/skills/personal/"],
        memory=["/memories/AGENTS.md"] if cfg.memory_enabled else None,
        permissions=permissions,
        tools=tools,
        subagents=subagents,
        interrupt_on=build_interrupt_on(cfg),
        checkpointer=checkpointer or MemorySaver(),
        name="harness",
    )
```

约束：

- `store` 只传 `create_deep_agent`。
- HITL 必须有 checkpointer，否则 pause/resume 丢状态。
- 子 agent 不自动继承主 agent 的 `skills`（`general-purpose` 除外）；要单独传。
- 子 agent 默认继承 `permissions` / `interrupt_on`；覆盖即整表替换。

## A.4 Backend 组装

`backend/agent/backends.py`：`build_backend` 按配置选路由，不在业务里手写 `CompositeBackend`。

```python
from deepagents.backends import CompositeBackend, StateBackend, StoreBackend, FilesystemBackend

def build_backend(cfg, *, store, context):
    routes = {
        "/memories/": StoreBackend(namespace=lambda _rt: ("memories", user_id(context))),
        "/policies/": StoreBackend(namespace=lambda rt: ("policies", org_id(rt))),
        "/skills/shared/": StoreBackend(namespace=lambda rt: ("skills-shared", org_id(rt))),
        "/skills/personal/": StoreBackend(namespace=lambda rt: ("skills-personal", user_id(rt))),
    }

    if cfg.sandbox.enabled:
        # 工作区进 Docker；内部临时文件留 StateBackend
        routes["/workspace/"] = DockerSandboxBackend(...)  # 见 3.6
    else:
        routes["/workspace/"] = FilesystemBackend(
            root_dir=cfg.workspace_root,  # 必须绝对路径
            virtual_mode=True,
        )

    return CompositeBackend(default=StateBackend(), routes=routes)
```

规则：

- default 必须是 `StateBackend`，避免 `/large_tool_results/` 等内部文件写进真实盘或容器根。
- 本地开发：`FilesystemBackend(virtual_mode=True)`，路径锁在 `root_dir`。
- 生产 / 要跑 shell：`/workspace/` 走 sandbox。sandbox 一旦作为 **Composite 的 default**，权限路径必须落在已知 route 前缀上；所以 sandbox 只挂 `/workspace/`，不要当 default。
- `StoreBackend.namespace` 用 runtime 上下文（`org_id` / `user.identity`），一次进程可多租户。

## A.5 Docker 沙箱

官方无内置 Docker provider。自实现 `BaseSandbox`，核心只做四件事：`id`、`execute`、`upload_files`、`download_files`。其余 `ls/read/write/edit/glob/grep` 基类用 `execute()` 拼脚本完成。

Agent 是否看到 `execute` 工具：backend 是否实现 `SandboxBackendProtocol`。非 sandbox 时不要自己再挂一套裸 `shell.py` 去打宿主机。

```python
from deepagents.backends.sandbox import BaseSandbox

class DockerSandboxBackend(BaseSandbox):
    def __init__(self, *, container_id: str, workdir: str = "/workspace"):
        self._container_id = container_id
        self._workdir = workdir

    @property
    def id(self) -> str:
        return self._container_id

    def execute(self, command: str, *, timeout: int | None = None):
        # docker exec -w /workspace <id> sh -lc <command>
        # 返回 ExecuteResponse(output, exit_code, truncated)
        ...

    def upload_files(self, files):
        # docker cp / put_archive -> 容器内路径
        ...

    def download_files(self, paths):
        # docker cp / get_archive <- 容器内路径
        ...
```

生命周期（`backend/scripts/` 或 factory 外围，不塞进 graph 节点）：

1. `docker run -d --rm`：无特权、只读根、工作目录可写、网络按配置关或白名单。
2. 会话开始：把 `/skills/**` 需要执行的脚本 `upload_files` 进容器（sandbox 外的 skill 文件默认进不去）。
3. 会话结束：下容器；镜像层可缓存，容器状态不复用（或按 TTL / snapshot 重置）。
4. 密钥不进镜像、不进环境变量明文。要注入必须最短寿命 + HITL 审 `execute`。

`tools/shell.py`：只做配置校验 / 包装，真正执行走 backend `execute`。禁止 `subprocess` 打宿主机。

Skill 脚本要跑：先同步再 `execute`。官方建议自定义 middleware 在模型调用前把 `/skills/**` 相关文件灌进 sandbox。

## A.6 Skills 管理

仓库目录是种子，不是运行时唯一真相。

```
backend/skills/
  example-skill/SKILL.md          # 开发期示例
运行时虚拟树：
  /skills/shared/<name>/SKILL.md  # 公共，只读
  /skills/personal/<name>/SKILL.md
```

每个 skill：目录 + `SKILL.md`（YAML frontmatter：`name`、`description`）+ 可选 `scripts/` `references/` `assets/`。

加载层级（`SkillsMiddleware`，传了 `skills=` 就有）：

1. 启动：只把每个 skill 的 `name` + `description` 打进 system prompt。
2. 命中：agent `read_file` 全文 `SKILL.md`。
3. 需要时再读 scripts / references。

公共 vs 个人：

| | 公共 `/skills/shared/` | 个人 `/skills/personal/` |
| --- | --- | --- |
| 谁写 | 应用代码 / 管理员种子，agent 不能写 | agent 可用 `write_file` / `edit_file` |
| 命名空间 | `("skills-shared", org_id)` | `("skills-personal", user_id)` |
| 权限 | `write` -> deny | 不 deny；可 `mode="interrupt"` |
| 覆盖 | 先列入 `skills=` | 后列入，同名覆盖公共 |

启动时：把仓库 `backend/skills/` 种子 `upload_files` / store put 到 `/skills/shared/`。个人库按 user namespace 隔离，不预置或只预置空目录。

`description` 必须写清「做什么 + 何时用」，否则同域 skill 会抢激活。`SKILL.md` 正文控制短，细节下沉 `references/`。

子 agent：自定义 subagent **不继承** 主 skills，要在 spec 里写自己的 `skills=[...]`。

## A.7 权限组装

`build_permissions`：列表顺序 = 匹配顺序，首个命中生效。更具体的规则必须在更宽泛的前面。无匹配则默认允许。

推荐默认表：

```python
from deepagents import FilesystemPermission

def build_permissions(cfg):
    return [
        # 1. 密钥 / 示例：全拒
        FilesystemPermission(
            operations=["read", "write"],
            paths=["/workspace/.env", "/workspace/**/.env", "/workspace/examples/**"],
            mode="deny",
        ),
        # 2. 公共 skill / 策略：只读
        FilesystemPermission(
            operations=["write"],
            paths=["/skills/shared/**", "/policies/**"],
            mode="deny",
        ),
        # 3. 个人 skill 写入：先审再写（deepagents>=0.6.8）
        FilesystemPermission(
            operations=["write"],
            paths=["/skills/personal/**"],
            mode="interrupt",
        ),
        # 4. 工作区：允许
        FilesystemPermission(
            operations=["read", "write"],
            paths=["/workspace/**"],
            mode="allow",
        ),
        # 5. 记忆：允许
        FilesystemPermission(
            operations=["read", "write"],
            paths=["/memories/**"],
            mode="allow",
        ),
        # 6. 兜底：拒
        FilesystemPermission(
            operations=["read", "write"],
            paths=["/**"],
            mode="deny",
        ),
    ]
```

操作别名：

- `read` = `read_file` + `ls`
- `write` = `write_file` + `edit_file`
- 细粒度还可写：`glob` `grep` `delete`

`delete` 不在 `write` 里，要删文件必须单独配。默认兜底 deny 后，未点名的 `delete` 会被挡。

`mode`：`allow` / `deny` / `interrupt`（指南前文 `interrpute` 以官方 `interrupt` 为准）。

权限只管内置 FS 工具。`execute` 在 sandbox 里等价任意 shell，路径规则拦不住 `cat /etc/passwd`。因此：

- sandbox 只挂 `/workspace/`
- `execute` 必须走 HITL 或命令白名单
- 容器禁权、断网或白名单

## A.8 Human-in-the-loop

两处可以暂停，resume 同一套：

1. `interrupt_on`：按工具名。设了就会挂 `HumanInTheLoopMiddleware`。
2. `FilesystemPermission(mode="interrupt")`：按路径截 `write_file` / `edit_file`。

```python
def build_interrupt_on(cfg):
    return {
        "execute": {"allowed_decisions": ["approve", "edit", "reject"]},
        "write_file": {
            "allowed_decisions": ["approve", "edit", "reject"],
            "when": lambda req: not str(req.tool_call["args"].get("file_path", "")).startswith("/workspace/"),
        },
        "edit_file": True,
        "delete": True,
        "read_file": False,
        "ls": False,
    }
```

决策：

| 决策 | 行为 |
| --- | --- |
| `approve` | 原参数执行 |
| `edit` | 改参数再执行 |
| `reject` | 不执行，把拒绝原因回给 agent |
| `respond` | 人当工具，回复当 tool result。不要用来否决有副作用的工具 |

运行：

1. `invoke` / `stream`，`config={"configurable": {"thread_id": ...}}`。
2. 出 interrupt -> UI 展示 tool + args。
3. `Command(resume=...)` 同 `thread_id` 再调。
4. 取消或中断发生在工具返回前：栈里的 `PatchToolCallsMiddleware` 会修消息历史。

注意：持久 shell session 当前不支持 interrupt。一次性 `execute` 可以。

## A.9 Subagents

`backend/subagents/researcher/AGENTS.md` 是指令源。`build_subagents` 读它，编成 `SubAgent` dict：

```python
researcher = {
    "name": "researcher",
    "description": "检索、整理资料；主 agent 只收结论。",
    "system_prompt": load_subagent_prompt("researcher"),  # 来自 AGENTS.md
    "tools": [web_search],            # 指定则整表覆盖，不再继承主工具
    "skills": ["/skills/shared/"],    # 自定义子 agent 必须自己传
    # permissions / interrupt_on 省略则继承主 agent
}
```

主 agent 用内置 `task` 工具委派，同步阻塞到子 agent 结束，只回收最终结果（上下文隔离）。

默认还有 `general-purpose` 子 agent（继承主 skills）。不要就关：harness profile 里 `GeneralPurposeSubagentProfile(enabled=False)`，且 `subagents=` 不传同步子 agent。不要用 `excluded_middleware` 拆 `SubAgentMiddleware`。

复杂图用 `CompiledSubAgent(name=..., description=..., runnable=compiled_graph)`，图逻辑放 `graph.py`。

## A.10 配置

`backend/config/schema.py` 最小字段：

```python
class SandboxConfig(...):
    enabled: bool = False
    image: str = "python:3.12-slim"
    network: str = "none"          # none | allowlist
    workdir: str = "/workspace"
    timeout_s: int = 60

class HarnessConfig(...):
    model: str
    workspace_root: str            # 绝对路径
    sandbox: SandboxConfig
    memory_enabled: bool = True
    hitl: bool = True
```

`default.yaml`：sandbox 关，HITL 开，本地 `FilesystemBackend`。  
`production.yaml`：sandbox 开，网络 none/白名单，checkpointer + store 换持久实现。

`backend/.env.example`：模型 key、Docker host、store DSN。密钥不进 YAML。

## A.11 实现顺序（Agent Core）

1. `schema.py` + `default.yaml`，能读出配置。
2. `build_backend`：先 State + Filesystem 的 `/workspace/`，单测路由。
3. `build_permissions` + `validators.py`，单测「首个匹配」和兜底 deny。
4. `create_harness_agent` 最小可 `invoke`（无 sandbox、无 HITL）。
5. skills 种子上传 + `/skills/shared/` 只读、`/skills/personal/` 可写。
6. checkpointer + `interrupt_on` + personal write `mode=interrupt`，写 resume 集成测。
7. `DockerSandboxBackend`，把 `/workspace/` 切到容器；补 skill -> sandbox 同步 middleware。
8. `execute` HITL；容器网络 / 权限收紧。
9. researcher subagent + `tools/web.py`。
10. logging / retry middleware；`eval.py` 跑固定任务。

未做 sandbox 前，`tools/shell.py` 保持空或直接报错，避免误打宿主机。
